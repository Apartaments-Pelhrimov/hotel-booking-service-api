spring:
  datasource:
    url:
    username:
    password:
  jpa:
    open-in-view: false
    show-sql: true
    hibernate.ddl-auto: validate
    properties:
      hibernate.generate_statistics: true
